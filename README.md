# 고유명사 지킴이

웹소설 원고를 맞춤법 검사기에 넣기 전에, 등장인물 이름·지명·세계관 용어를 임시 코드로 숨겼다가
검사가 끝나면 원래대로 되돌려 주는 도구입니다. 파일 하나(`index.html`)로 동작합니다.

**바로 사용하기: https://kurotoni.github.io/novel-spell-checker/**

© 2026 KUROTONI 쿠로토니. All rights reserved.
개인 사용은 자유입니다. 재배포와 판매는 금지합니다. 자세한 내용은 [LICENSE](LICENSE)를 확인하세요.

---

## 1. 이 도구가 필요한 이유

맞춤법 검사기는 `루시아`, `에스페란자 왕국` 같은 창작 고유명사를 모릅니다. 그래서

- 멀쩡한 이름을 비슷한 단어로 "고쳐" 버리고
- 이름 뒤의 조사(은/는, 이/가, 을/를)를 엉뚱하게 바꿔 놓습니다.

그래서 이 도구는 **이름과 그 뒤에 붙은 조사를 한 덩어리로 숨깁니다.**

```
원본 : 루시아가 에스페란자 왕국으로 갔다.
치환 : [[PROTECTED_1]] [[PROTECTED_2]] 갔다.
복원 : 루시아가 에스페란자 왕국으로 갔다.
```

조사를 밖에 남겨 두면 검사기가 `[[PROTECTED_1]]가`의 `가`를 `이`로 바꾸는 사고가 납니다.
대신 **조사 맞춤법은 이 앱이 직접 검사**합니다. 받침 규칙을 알고 있으므로 검사기보다 정확합니다.

| 잘못 쓴 예 | 앱의 지적 |
| --- | --- |
| 루시아이 | 받침 없음 → `가` |
| 마나 심장를 | 받침 있음 → `을` |
| 아르실으로 | ㄹ 받침 → `로` |
| 루시아님이 | 호칭 `님`의 받침 기준으로 판정 (정상) |

---

## 2. 사용 순서

1. **왼쪽 사전에 고유명사 등록** - 입력 후 Enter, 또는 쉼표로 여러 개 한 번에. 브라우저에 자동 저장됩니다.
2. **1단계에 원고 붙여넣기** - 조사가 틀린 곳이 있으면 노란 상자로 알려 줍니다. `고치기` 또는 `모두 교정`.
3. **`고유명사 숨기기 (치환) + 복사`** - 치환된 글이 클립보드에 자동 복사됩니다.
4. **2단계의 검사기 링크 열기** - 붙여넣고 맞춤법 검사를 받습니다.
5. **고쳐진 글을 3단계에 붙여넣고 `되돌리기`** - 이름이 원래대로 돌아옵니다.
6. **`최종 원고 복사하기`**

### 주의

- 치환표(번호 ↔ 원래 이름)는 이 브라우저에만 저장됩니다. **시크릿 창을 닫거나 브라우저 데이터를 지우면 사라집니다.**
  1단계와 3단계는 되도록 한 번에 마치세요. (새로고침이나 브라우저 재시작은 괜찮습니다.)
- 검사기가 `[[PROTECTED_1]]`을 통째로 삭제하는 경우가 드물게 있습니다.
  복원 후 "사라진 것으로 보이는 고유명사" 안내가 뜨면 그 부분을 직접 확인하세요.
- 네이버 검사기는 한 번에 넣을 수 있는 글자 수에 제한이 있습니다. 길면 나눠서 검사하고,
  치환 코드가 글자 수를 많이 차지하면 왼쪽 `치환 코드 모양`을 `[[1]] (짧게)`로 바꾸세요.

---

## 3. GitHub Pages 배포 방법 (비개발자용)

1. GitHub에 로그인하고 오른쪽 위 `+` → **New repository**.
2. Repository name에 예를 들어 `noun-guard` 를 넣고, **Public**을 선택한 뒤 **Create repository**.
3. 만들어진 화면에서 **uploading an existing file** 링크를 누릅니다.
4. `index.html` 파일을 끌어다 놓고 아래 **Commit changes** 버튼을 누릅니다.
5. 위쪽 **Settings** 탭 → 왼쪽 메뉴 **Pages**.
6. `Branch` 항목에서 **main** 과 **/ (root)** 를 선택하고 **Save**.
7. 1~2분 뒤 새로고침하면 주소가 나옵니다.
   `https://<내아이디>.github.io/noun-guard/`

파일을 고쳤으면, 저장소 화면에서 `index.html`을 클릭 → 연필 아이콘 → 수정 → Commit 하면 몇 분 뒤 반영됩니다.

> 참고: `index.html`을 바탕화면에 두고 더블클릭해도 그대로 동작합니다. 다만 이때는
> 자동 복사가 막히는 브라우저가 있어 `복사` 버튼을 한 번 더 눌러야 할 수 있습니다.
> GitHub Pages(https)에 올리면 자동 복사까지 정상 동작합니다.

---

## 4. 코드 수정 가이드

`index.html` 하나에 화면과 기능이 모두 들어 있습니다. 아래 `<script>` 안은 `[1] ~ [17]` 번호로
구역이 나뉘어 있으니, 브라우저나 메모장에서 `[2]` 처럼 검색해서 찾으면 됩니다.

### 조사를 더 넣고 싶을 때 → 구역 `[2]`

```js
const PARTICLE_PAIRS = [
  ['은','는'], ['이','가'], ...   // [받침 있을 때, 받침 없을 때]
];
const PARTICLE_PAIRS_RO = [
  ['으로','로'], ...              // ㄹ 받침 뒤에서는 '로'를 쓰는 무리
];
const PARTICLES_FIXED = [
  '의','에','에게','까지', ...    // 모양이 바뀌지 않는 조사
];
const SUFFIXES = ['님','씨','양','군','짱','쨩'];  // 이름 뒤 호칭
```

`PARTICLES_FIXED`에 `'라며'`를 추가하고 싶다면 목록 안에 `'라며',` 를 넣으면 됩니다.
받침에 따라 모양이 바뀌는 조사라면 `PARTICLE_PAIRS`에 `['이라며','라며']` 형태로 넣으세요.

### 치환 코드 모양을 바꾸고 싶을 때 → 구역 `[7]`

```js
function makePlaceholder(id, style) {
  return style === 'short' ? '[[' + id + ']]' : '[[PROTECTED_' + id + ']]';
}
```

여기만 고치면 됩니다. 단, 되돌리기 규칙(`RE_FULL`, `RE_SHORT`, `RE_LOOSE`)이 바로 아래에 있으므로
모양을 완전히 새로 만들면 그 규칙도 함께 고쳐야 합니다. 되도록 대괄호 형태는 유지하세요.

### 원고지 계산 기준을 바꾸고 싶을 때 → 구역 `[8]`

```js
const sheets = (all / 200);   // 200자 원고지 기준
```

`200`을 `400` 등으로 바꾸면 됩니다.

### 맞춤법 검사기 링크를 추가하고 싶을 때

HTML에서 `네이버 맞춤법 검사기 열기` 를 검색하면 링크 버튼이 나옵니다.
그 `<a ...>` 줄을 복사해 `href="..."` 와 글자만 바꾸면 버튼이 하나 더 생깁니다.

### 색을 바꾸고 싶을 때

Tailwind CSS 색 이름을 씁니다. 예를 들어 `bg-indigo-600`(보라 계열 버튼)을 `bg-rose-600`으로
모두 바꾸면 강조색이 붉은 계열로 바뀝니다. 사용 가능한 색: slate, gray, red, rose, amber, emerald,
teal, sky, blue, indigo, violet, fuchsia 등이며 숫자는 50(밝음)~950(어두움)입니다.

---

## 5. 자주 묻는 질문

**Q. 내 원고가 어디로 전송되나요?**
아니요. 모든 처리는 브라우저 안에서만 이뤄지고 서버로 보내지 않습니다. (2단계에서 검사기 사이트에
직접 붙여넣는 것은 별개입니다.)

**Q. '검'을 등록했더니 '검색'까지 바뀔까 봐 걱정됩니다.**
`단어 경계 확인` 옵션이 켜져 있으면 뒤에 조사가 아닌 글자가 붙은 경우 건드리지 않습니다.
`검색`은 그대로 두고 `검을`, `검은` 만 치환합니다.

**Q. 두 이름이 겹칩니다. (루시아 / 루시아나)**
항상 긴 이름을 먼저 찾습니다. `루시아나는` 은 `루시아나 + 는` 으로 처리됩니다.

**Q. 인터넷이 끊긴 상태에서도 되나요?**
디자인(Tailwind CSS)을 인터넷에서 받아오므로, 오프라인에서는 화면이 흐트러집니다.
기능 자체는 동작합니다.

---

## 6. 저작권 및 이용 약관

© 2026 KUROTONI (쿠로토니). All rights reserved.

이 소프트웨어는 오픈 소스가 아닙니다. 소스 코드는 공개되어 있지만, 아래 범위를 벗어난 이용은 허락되지 않습니다.

**허용**
- 개인적인 용도의 자유로운 사용 (무료)
- 이 도구로 작업한 원고의 상업적 출판 및 연재. 결과물의 권리는 전적으로 사용자에게 있습니다
- 소스 코드 열람, 본인이 쓸 목적의 수정

**금지**
- 재배포 (원본이든 수정본이든, 다른 주소에 올려 공개하는 것 포함)
- 판매, 유료 서비스화, 광고를 통한 수익화
- 저작권 표시 제거 또는 변경

전문은 [LICENSE](LICENSE) 파일에 있습니다. 앱 화면 아래쪽 `이용 약관 전문` 버튼으로도 볼 수 있습니다.

이 소프트웨어는 있는 그대로 제공되며, 원고 손실을 포함한 어떠한 손해에 대해서도 저작권자는 책임지지 않습니다.
**중요한 원고는 반드시 별도로 백업하십시오.**

---

## 7. 서식 유지와 내장 검사 (v2)

### 왜 줄바꿈이 사라졌나

외부 맞춤법 검사기는 결과를 돌려줄 때 줄바꿈을 없애 원고를 줄글로 만들어 버리는 경우가 있습니다.
고유명사 치환 자체는 원문을 그대로 두므로 서식이 깨지지 않습니다. 문제는 외부 검사기 쪽입니다.

### 해결 1 · 사이트 안에서 검사 (경로 A)

1단계에 원고를 넣으면 브라우저 안에서 바로 검사가 돌아갑니다. 외부 사이트를 거치지 않으므로
줄바꿈, 빈 줄, 들여쓰기, 대사가 전혀 손상되지 않습니다. 교정한 뒤 `원고 복사`를 누르면 끝입니다.

검사 항목은 네 가지로 분류됩니다.

| 분류 | 예 |
| --- | --- |
| 조사 | 루시아이 → 루시아가, 아르실으로 → 아르실로 |
| 맞춤법 | 됬 → 됐, 몇일 → 며칠, 오랫만 → 오랜만 (약 60개 규칙) |
| 띄어쓰기 | 할수있다 → 할 수 있다, 갈것같다 → 갈 것 같다 |
| 문장부호 | ... → …, 겹친 느낌표, 짝이 안 맞는 큰따옴표 |

`오류` 표시는 확실한 오류라 `확실한 오류 모두 교정`으로 한 번에 고칠 수 있고,
`확인` 표시는 문맥에 따라 달라지는 항목이라 직접 판단해 하나씩 고칩니다.

내장 검사는 전문 맞춤법 검사기만큼 넓게 보지는 못합니다. 자주 틀리는 표현을 빠르게 걸러 내는 용도입니다.

### 해결 2 · 줄바꿈 보호 (경로 B)

외부 검사기를 계속 쓰고 싶을 때를 위한 장치입니다. 치환할 때 각 줄 앞에 줄 표시를 함께 넣습니다.

```
원본 : "안녕."
치환 : [[L3]]"[[PROTECTED_2]]."
```

외부 검사기가 줄바꿈을 전부 없애 놓아도, 3단계에서 이 표시를 기준으로 원래 줄 구조를 정확히
되살립니다. 빈 줄과 들여쓰기도 그대로 복구됩니다. 왼쪽 `치환 옵션`의 `줄바꿈 보호`로 켜고 끕니다.

줄 표시가 일부 사라지면 몇 번째 줄이 영향을 받았는지 알려 줍니다. 그 줄의 내용은 앞줄에 붙어
있으니 직접 나누면 됩니다. 내용이 사라지지는 않습니다.

### 맞춤법 규칙을 추가하고 싶을 때 → 구역 `[3-B]`

```js
const SPELL_RULES = [
  { re: /됬/g, to: '됐', why: "'됬'은 없는 표기입니다.", level: 'high', tag: '맞춤법' },
];
```

`level`이 `high`면 `모두 교정` 대상이고, `check`면 사용자가 직접 확인해 고치는 항목입니다.
등록된 고유명사 구간은 검사에서 자동으로 제외되므로, 인물 이름이 오류로 잡히지 않습니다.
